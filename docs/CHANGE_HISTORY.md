# Change History — Features & Bug Fixes

This document tracks notable **features** and **bug fixes** across the BeRL / TomRL project, derived from git history. Each row points to the originating commit so changes can be traced back to source.

> **Maintenance rule:** Every time a code change is made, add a row to the relevant table below with the commit hash, a short description of *what* changed and *why*. See `CLAUDE.md` → "Keeping Change History Updated".

## Features

| Commit | Date | Feature | What changed | Why |
|--------|------|---------|--------------|-----|
| `0ee87fc` | 2026-07-07 | Anti-collapse training knobs for dialogue frozen-RM GRPO (`think_only_pg` + `format_penalty`) | **Fix 1 (`actor.think_only_pg`, default False):** restrict the policy-gradient loss + entropy to `<think>...</think>` tokens while the KL term keeps the FULL response mask. `fsdp_workers.py::ActorRolloutRefWorker` adds `_compute_think_mask` (decode valid response → `rfind("</think>")` → re-encode prefix len → zero the answer span; no-tag rows keep full mask) and attaches `data.batch['think_mask']` in `update_actor` when enabled. `dp_actor.py::update_policy` selects `think_mask`, uses it as `eos_mask` for `compute_policy_loss` + the entropy `masked_mean`, and keeps `response_mask` for the KL `masked_mean`. **Fix 2 (`reward_model.format_penalty` / `actor.format_penalty`, default 0.0):** graded penalty subtracted from the reward when the model's OWN response is malformed. `response_parser.py` adds `has_format_violation(response)` (not exactly one `<think>`/`</think>` pair, or empty/malformed answer — for tag-free parsers requires non-empty content after `</think>` with stray `</?answer>` stripped). Both RM paths (`_switch_chat_template`/`_forward_micro_batch` frozen; `_build_actor_rm_inputs`/`_actor_rm_forward` actor-as-RM) collect `format_violation` per sample and subtract `format_penalty`; final clamp to [MIN_REWARD, MAX_REWARD] bounds it. Config defaults added to `ppo_trainer.yaml` | The Gemma2-2b dialogue frozen-RM run peaked at step 30 (tomi 0.67, explore_tom 0.66) then collapsed by step 60 (tomi 0.26, ~59% empty `</answer>` outputs). Root cause: the dialogue reward scores only `<think>` + the TRUE next utterance (the model's own answer is discarded), then tiles that scalar over ALL response tokens — so answer tokens get gradient uncorrelated with answer quality and drift to empty under weak KL=0.05 + a positive entropy bonus. Fix 1 stops training the (reward-blind) answer span while KL still anchors its format to base; Fix 2 gives an explicit, graded signal against malformed responses. The user explicitly rejected early-stopping — the goal is to prevent eval regression via training-side fixes | | `experiments/lib/common.sh`: guarded `berl::write_summary_md` behind `BERL_NO_EXP_LOG` (default 0) so verification/test runs don't create/append `experiments_logs/<RUN_NAME_BASE>.md`. Documented the flag + `VAL_SUITE`/`VAL_METRIC_SUFFIX` in `experiments/README.md`, and clarified in `launch-experiment` §4 that "run without logging" means skip the experiment logs/summaries — **not** WandB (which is on for every run) | The user clarified: "without logging" = skip the changelog/reproducibility-md/tracker bookkeeping, but "ALWAYS include wandb for any run". A verification run must still log to WandB while leaving the experiment-tracking artifacts untouched |
| _pending_ | 2026-07-06 | Per-suite val metric suffix (`data.val_metric_suffix`) | Added `data.val_metric_suffix` (default `''`) to `verl/trainer/config/ppo_trainer.yaml`; `ray_trainer.py::_validate` now logs `val/test_score/<data_source><suffix>`. `common.sh` sets it per `VAL_SUITE`: `subsample300`→`_sub300`, `core`→`_core`, `sanity`→`_sanity`, `full`→'' (canonical); overridable via `VAL_METRIC_SUFFIX`. Passed through as `data.val_metric_suffix` in the launcher ARGS | With `subsample300` reusing the same `data_source` values (tomi, explore_tom, …) as the full suite, a subset run's WandB series `val/test_score/tomi` would overwrite/blur the full-suite series for the same benchmark. Suffixing subset metrics keeps the two comparable-but-distinct in WandB; the full suite retains the canonical names for headline reporting |
| _pending_ | 2026-07-06 | Representative stratified eval subset (`VAL_SUITE=subsample300`, new default) | Added `examples/data_preprocess/build_eval_subsample.py` — seeded stratified sampler that takes `min(300, available)` rows per `data_source` across the 9 benchmark files (25 subtypes, `ullman_perturbed` excluded → 7,500 prompts) and writes `data/cleaned_tom/eval_subsample_300.parquet`. Wired a `subsample300` case into the `common.sh` `VAL_SUITE` switch and made it the **new default** (was `full`); `full`/`core`/`sanity` remain. Documented a "Selecting the eval set" section (per-suite prompt counts + measured eval times + the `≈11s + 0.0117s×N` time model) in `docs/EVAL_BENCHMARKS.md` | The full suite (~40k prompts, ~8 min/eval) is too slow to run every `TEST_FREQ`; a stratified N=300/subtype subset gives a ~3.5pp per-subtype 95% CI — far tighter than the 5–15pp effects the project reports — while covering every benchmark family and running ~5× faster (~1.6 min). Fixed seed + identical subset across runs keeps experiments comparable; full suite is reserved for start/end headline reporting |
| _pending_ | 2026-07-06 | Full eval-suite default (`VAL_SUITE`) + 6k smoke mix | `experiments/lib/common.sh`: replaced the hardcoded 2-file `VAL_FILES` default with a `VAL_SUITE` switch — `full` (all 10 benchmark parquets — tomi/explore/hi + fantom + bigtom/dyntom/exploretom_infilled/mmlu/opentom/simpletom/tombench/ullman), `core` (legacy tomi+fantom), `sanity` (`eval_suite_sanity.parquet`, ~40/subtype); an explicit `VAL_FILES=[...]` still bypasses. Grew `dcfg_smoke_mix`(+`_gemma`) from 470→6100 rows (dailydialog/empathetic/cga 1000 each, negotiation 500-600, diplomacy 400, thoughttrace 600) and rebuilt both parquets (all 9 sources, `answer_pp` fully populated) | The smoke/verification runs only reported 8 eval scores (the 2 wired files) and made few steps (470 rows). Making the full suite available surfaces regressions/reward issues across every benchmark family; 6100 rows gives ~190 steps/epoch so a run is meaningful. (Default later switched to `subsample300` — see row above) |
| _pending_ | 2026-07-05 | `dcfg_*` data configs with `extends` inheritance (Phase 4 Section B) | Added config inheritance to `build_dataset.py`: `load_config` resolves an optional `extends:` parent (single path or list, relative to the file, deep-merged child-wins, cycle-guarded) and a top-level `dataset_defaults:` block applied to every dataset entry (precedence: `DATASET_DEFAULTS` → `dataset_defaults` → pipeline `seed` → per-entry). Created `dcfg_base.yaml` (shared defaults per the project data-gen convention: `min_turns:6`, `min_response_words:3`, `max_response_words:100`, `turn_order:random`, `simple`/`cot_eval`, `split:train`) and `dcfg_base_notags.yaml` (tag-free for Qwen3/Gemma: `cot_eval_notags` + `add_response_tags:false`). Added thin per-domain configs (`dcfg_empathetic/dailydialog/cga/casino/craigslist/diplomacy/p4g/thoughttrace`), mixes (`dcfg_mix_all`), and smoke mixes (`dcfg_smoke_mix`, `dcfg_smoke_mix_gemma`); negotiation sources carry a justified `min_response_words:1` override (whole-conversation filter otherwise drops CaSiNo). Deleted 6 superseded `pipeline_config_*.yaml` (kept `pipeline_config.yaml` as the full-schema example). Fixed smoke launcher DATA_TRAIN/DATA_NAME defaults to the dcfg parquets. Updated `docs/skill_adding_dataset.md` (Step 3 rewritten for `dcfg_*`+`extends`) and `launch-experiment` §4b | Config filename stem = `output_name` = tracker `Data config name` = parquet stem, the 1:1 mapping `run_experiment.sh` relies on. `extends` removes the ~30-line duplicated parameter block per config so a new domain/mix is just a `datasets:` list; the shared defaults enforce the data-gen convention in one place. `dcfg_smoke_mix` gives the Phase-stability phase a small fixed all-domain mix that follows the common params |
| _pending_ | 2026-07-05 | Generic task×model-class experiment launchers + tracker dispatcher (Phase 4) | Replaced 13 round-specific training/smoke scripts with a single parameterised `main_ppo` code path: `experiments/lib/common.sh` (env setup with **WandB online** — never unsets `WANDB_API_KEY`; model-family branch; `RUN_NAME` build with auto `-r<N>`; hard-default `+data.truncation=left`; `GPU_IDS` pinning; `BERL_DRY_RUN`; auto-emits the reproducibility md) + thin launchers `train_{behavior,tom}_{qwen2.5,qwen3,gemma}.sh`, `smoke_{qwen2.5,qwen3,gemma}.sh`, `train_sft_qwen2.5.sh` (build stub). Added `scripts/tracker_to_sidecar.py` (tracker md → `project_planning/experiments.tsv`, 92 rows) and `experiments/run_experiment.sh <EXP_ID>` (dispatcher: reads sidecar → maps knobs to env → routes to launcher; caller env WINS over sidecar to resolve placeholders; numeric guards reject unresolved KL/LR placeholders). Added `experiments/README.md`; repointed skill/docs references to the new launchers | Locked Phase-4 decisions: the 92 tracker experiments are the same invocation with different knobs, so a maintained matrix of launchers + a dispatcher beats 92 one-off scripts. Verified every launcher reproduces the corresponding known-good command via `BERL_DRY_RUN=1` (behavior/tom/gemma-quirks all match). Each run now writes a self-contained reproducibility record for the paper |
| _pending_ | 2026-07-05 | Tag-free CoT prompt + answer-tag consistency guard for Qwen3/Gemma | Added `cot_eval_notags` system-prompt style (CoT + ToM framing, **no** `<answer>` instruction) and `validate_prompt_tag_consistency()` in `scripts/prompt_templates.py`; wired a **fail-fast** guard into `build_dataset.py` (and a warn-only guard into `DialogueConverterBase`) that rejects configs where `system_prompt_style` (does it instruct `<answer>`?), `add_response_tags`, and `generation_prefix` disagree. Added `scripts/configs/pipeline_config_all_dialogue_notags.yaml` (`cot_eval_notags` + `add_response_tags:false` + empty `generation_prefix`) for native-thinking models. Switched all pipeline configs to the new `simple` user-prompt style. Documented the mandatory tagged-for-Qwen2.5 / tag-free-for-Qwen3+Gemma rule in `launch-experiment` §4b. Added `scripts/make_tag_test_datasets.py` to build paired tagged/tag-free samples from `all_dialogue` | The scorer's `<answer>`-tag expectation is set by **model type** (`response_parser.py`: Qwen2 requires tags, Qwen3/Gemma don't), while the prompt-side (`system_prompt_style`) and target-side (`add_response_tags`) tags were configured independently with no validation — and the `+require_answer_tags=` overrides are no-ops. A tag-free dataset paired with a tag-demanding `cot_eval` prompt would silently mismatch prompt/target/reward. The guard makes such a mismatch impossible to build, and `cot_eval_notags` gives native-thinking models a consistent tag-free recipe | Added `launch-experiment` skill §4a and a "Prompt alignment (train vs eval)" section to `docs/EVAL_BENCHMARKS.md`: any run that varies the CoT/system prompt MUST pass `+data.system_prompt="<text>"` so the override replaces the system message in both the train and val loaders (aligning training with the `cot_eval`-baked evals) | Evals bake `cot_eval` and no run sets `data.system_prompt`, so changing the training `system_prompt_style` silently re-creates the Round-17 train/eval mismatch. Documenting Option A makes agents keep the comparison fair |
| _pending_ | 2026-07-05 | Self-describing WandB run-name convention (RQ + data + params + run index) | New naming scheme `<RQ>-<expid>-<data_name>-<model>-<params>-r<N>` (single hyphen). Baked the `RQ tag` into every tracker `Exp ID` and rebuilt all 92 `Run name` cells to `<RQ>-<expid>-<data_name>`; documented the scheme + `test` RQ for smoke runs + `r<N>` run index (bumped per resubmission to avoid clashes) in `BeRL_experiments_tracker.md` §"Agent protocol" #4 and §"Column meanings". Updated skills `launch-experiment` (run-name construction + next-free run-index pick), `join-experiments`/`log-results` (per-run md keyed by base stem `<RUN_NAME_BASE>` so all attempts append to one file). Wired `RQ_TAG`/`RUN_INDEX` env vars into `experiments/qwen2.5_3b_all_dialogue.sh`, `tom_grpo_power_reward.sh`, and set `RQ=test` in the smoke tests | The WandB dashboard only shows the run name; the old names omitted the research question and data config, so a run's purpose/data/params weren't visible at a glance. A run index lets a crashed run be resubmitted (`-r2`, `-r3`) without clashing with the crashed `-r1` in WandB/log filenames |
| _pending_ | 2026-07-06 | Eval benchmark catalog doc | Added `docs/EVAL_BENCHMARKS.md` documenting every ToM eval (purpose, source, rows, format, prep script, parquet, `data_source` tags, scorer, Qwen2.5-3B baselines) plus the 3 flagged/blocked evals and regeneration commands | Make the eval suite self-documenting so agents/reviewers can see what each benchmark tests and how it is scored without reading every prep script |
| _pending_ | 2026-07-06 | 8 new ToM eval benchmarks + shared eval scorer | Added shared eval-prep helper `examples/data_preprocess/tom_eval_common.py` (cot_eval prompt, choice formatting, ground-truth JSON, row schema, parquet writer) and generic scorer `verl/utils/reward_score/tom_mc.py` (MC/binary/exact/list, reuses explore_tom + fantom helpers). New prep scripts: `prepare_simpletom.py` (allenai/SimpleToM), `prepare_tombench.py` (ycfNTU/tombench_merged), `prepare_opentom.py` (SeacowX/OpenToM), `prepare_bigtom.py` (GitHub cicl-stanford/procedural-evals-tom, reconstructs forward/backward belief+action conditions), `prepare_dyntom.py` (YangXiao-nlp/DynToM), `prepare_mmlu.py` (cais/mmlu guardrail), `prepare_ullman_perturbed.py` (Ullman false-belief perturbations), `prepare_exploretom_infilled.py` (facebook/ExploreToM natural-language robustness variant). Wired new `data_source`s into `verl/trainer/main_ppo.py::_select_rm_score_fn`. Added tests `tests/reward_score/test_tom_mc.py` (unit) and `tests/reward_score/test_eval_parquets.py` (integration: gold scores +, wrong scores ≤0) — all pass | Paper eval suite (`project_planning/BeRL_paper_plan.md`) required OOD/Applied/Dynamic/Robustness/guardrail benchmarks that were missing. DialToM (no locatable data), Mental-State-Representation probe (needs hidden-state probing), and Behavior-PPL (training-side LL metric) are flagged for user review as not implementable via the rule-based parquet+scorer protocol | `experiments/gemma2_2b_all_dialogue.sh`: switched `USE_ACTOR_AS_RM=True→False` (frozen copy of base Gemma2 as RM, `reward_model.enable=True`, `type=lm`, `model.path=$model_name`) with the power reward (`REWARD_TYPE=power`, `POWER_K=2.0`, `POWER_LL_MIN=-8.0`), `EXP_DESC` → `power-reward-flash-fantom` (`EXP_NAME` auto-derives `frozenRM`). Added RM memory hedges: `reward_model.model.fsdp_config.param_offload=True`, `reward_model.micro_batch_size=4`, and crucially `reward_model.forward_max_token_len_per_gpu=3072`. Verified step-0 eval matches baseline (tomi 0.615, explore_tom 0.352, hi_tom 0.132), no OOM, and stable training (reward −14.6→−2.7 over 5 steps, response_length steady ~210, entropy ~1.5) | The actor-as-RM run (`kc6sjkye`) diverged ~step 185: reward = actor's own mean log-prob is a moving target the policy games by lengthening/sharpening its outputs → response_length 120→550, entropy 0.6→4.3, reward→−15, tomi eval collapse 0.66→0.27. A frozen RM's opinion is fixed, breaking the feedback loop. The RM inherits `use_dynamic_bsz=True` from the actor and (via critic) `forward_max_token_len_per_gpu=32768`, so it ignored `micro_batch_size` and packed 32k tokens → with Gemma2's 256k vocab + fp32 log-softmax upcast that is a ~47 GiB single allocation → OOM; capping the RM forward to 3072 tokens (matching the actor cap) fixes it | `experiments/gemma2_2b_all_dialogue.sh`: switched from `attn_implementation=eager` + `use_remove_padding=False` to `attn_implementation=flash_attention_2` + `use_remove_padding=True` (matching the Qwen recipe). Verified step-0 eval is byte-identical to the eager baseline (tomi 0.615, explore_tom 0.351, hi_tom 0.132, fantom_info_binary 0.404, fantom_answ_binary 0.249) and rollouts stay non-empty (response_length ~210). Per-GPU memory dropped from ~77GB peaks (which OOM'd at step 80 even on 8 GPUs) to a steady ~27GB. `use_dynamic_bsz`+`ppo_max_token_len_per_gpu=3072` kept as a safety cap | The eager+no-rmpad combo was forced because OLD flash-attn ignored Gemma2's attention soft-capping (→ logits diverge from the soft-capped vLLM rollout → empty-output collapse). But `flash_attn 2.8.3` + `transformers 4.51.3` DO apply the soft-cap in the flash path (`flash_attn_varlen_func` takes a `softcap` arg; HF Gemma2 passes `attn_logit_softcapping`; `final_logit_softcapping` is applied at the LM head regardless of backend), and verl registers `gemma2` as rmpad-capable. Without rmpad, sequences are padded and the LM head materializes a `[batch, seq, 256k-vocab]` logits+backward tensor (Gemma2 vocab 256k vs Qwen 152k) → ~8GB spike → OOM. This is why a 2B model OOM'd while an 8B Qwen (packed, rmpad) did not — a config problem, not model size |
| _pending_ | 2026-07-04 | Gemma2-2b GRPO stabilization + warm-start & auto-restart supervisor | `experiments/gemma2_2b_all_dialogue.sh`: log-prob reward (Qwen-matched, dropped power-reward overrides), `attn_implementation=eager` + `use_remove_padding=False`, `use_dynamic_bsz=True` + `ppo_max_token_len_per_gpu=3072`, GPU pinning via new `GEMMA_GPUS` var (SLURM presets `CUDA_VISIBLE_DEVICES`), and `MODEL_TAG`/`GEMMA_INIT_MODEL` split so restarts can warm-start from a checkpoint while keeping a stable `EXP_NAME` (`...-logprob-reward-eager-fantom`), and `save_freq` lowered 50→10 so a checkpoint banks before the recurring ~step-20 death, and default GPUs switched to all 8 (`0-7`) to lower per-GPU memory. New `experiments/gemma2_supervisor.sh`: waits for ≥8 free GPUs, prefers the HIGHEST-index free GPUs, has a guard so it never double-launches when training is already alive, launches training pinned, warm-starts from the NEWEST-mtime matching `global_step_*` checkpoint, retries on death, stops on the `Final validation metrics` marker | Gemma2's soft-capping needs eager attention to match the vLLM FLASH_ATTN rollout, but eager forbids rmpad packing → non-rmpad 256k-vocab entropy blows up memory; dynamic token-budget batching caps peak memory. The ~73GB/GPU OOMs were NOT an external squatter — they were our OWN FSDP workers peaking (the OOM message shows memory "allocated by PyTorch" against our own worker PIDs; the node is exclusively ours with all 8 GPUs). Using all 8 GPUs adds dp ranks so each GPU holds a smaller batch slice → lower activation peak (first 8-GPU run reached step 44 vs ~20 on 4). verl has no training-state resume, so the supervisor + checkpoint warm-start makes the ~1,600-step run cumulative across interruptions |
| _pending_ | 2026-07-03 | Qwen3 vLLM rollout support | Added `verl/third_party/vllm/vllm_v_0_6_3/qwen3.py` (Qwen2 + per-head QK-RMSNorm + `attention_bias=False` + explicit `head_dim`) and registered `Qwen3ForCausalLM` in vLLM's `ModelRegistry` from `verl/third_party/vllm/__init__.py`; added `experiments/qwen3_1.7b_all_dialogue.sh` | vLLM 0.6.3 predates Qwen3 and had no `Qwen3ForCausalLM` in its registry, so rollout crashed at model-build time (`ValueError: Model architectures ['Qwen3ForCausalLM'] are not supported`). Prior "Qwen3 support" (`f9a9d18`, `7590ebd`) only covered FSDP/HF training + dtensor weight bridging; the vLLM rollout model was only ever added to the now-deleted `tom2`/`tom3` envs. Verified: QK-norm weights load correctly and step-0 baselines match Round 20 (tomi 65.0, hi_tom 37.8, explore_tom 48.4) |
| _pending_ | 2026-06-30 | Per-speaker prediction target (`target_speaker`) | `DialogueConverterBase` accepts `target_speaker`; when set, word-count limits are enforced only on the target turn and only that speaker's turns become prediction examples; enabled `target_speaker: "user"` for thoughttrace in `pipeline_config_all_dialogue.yaml` | Human–AI sources (ThoughtTrace) have very long AI turns (~231w) that rejected whole conversations under `max_response_words`; predicting only the human's turns recovers usable examples (was 7/3000) |
| _pending_ | 2026-06-29 | New training datasets | Added 7 converters (CaSiNo, CraigslistBargain, DealOrNoDeal, Switchboard, Diplomacy, PersuasionForGood, ThoughtTrace) via shared `DialogueConverterBase`/`ConvoKitConverterBase`; registered in `build_dataset.py`; `pipeline_config_negotiation_tom.yaml` | Expand dialogue→ToM training sources per docs/NEW_DATASETS.md (negotiation/deception = high ToM signal) |
| `8d4d781` | 2026-06-27 | Environment setup guide | Added `docs/ENVIRONMENT_SETUP.md` documenting the canonical conda env | Remove ambiguity about which env to use for training/eval |
| `2cbb38e` | 2026-06-23 | Misc utility files | Added helper files | Support day-to-day workflow |
| `7590ebd` | 2026-06-11 | FANToM/CGA reward scoring + dataset docs | Reward scoring for FANToM/CGA, Qwen3 support, dialogue dataset scripts and extensive docs | Enable multi-party ToM eval + new training sources |
| `063a5e6` | 2026-06-03 | Runtime system prompt injection + Gemma2 | Inject system prompt at runtime via `rl_dataset.py`/`ray_trainer.py`; Gemma2 experiment scripts | Avoid baking prompt into data; support new model family |
| `f9a9d18` | 2026-05-31 | Qwen3 compatibility | Optional `<answer>` tags, adaptive stitching, `verl/models/transformers/qwen3.py` | Support Qwen3 native think-mode |
| `b27a164` | 2026-05-24 | FANToM eval benchmark | `prepare_fantom.py`, `reward_score/fantom.py`, eval scripts | Add multi-party conversation ToM benchmark |
| `3dafe6c` | 2026-05-20 | Repo reorganization | Moved experiment scripts to `experiments/`, pipeline configs to `scripts/configs/` | Cleaner structure |
| `3902460` | 2026-05-20 | Training sample logging | Log samples with step/source/score context | Debug reward & data quality during runs |
| `512704d` | 2026-05-26 | Conversations Gone Awry dataset | CGA converter + `docs/skill_adding_dataset.md` | New adversarial training source |
| `2a534a9` | 2026-05-06 | Wider clip + actor-as-RM | Reward clipping (-40,40), actor-as-RM mode | Round 17e-g best configs |
| `378863f` | 2026-05-05 | Dialogue→ToM transfer breakthrough | System prompt alignment (`cot_eval`), power reward | First positive ToM transfer |
| `ce37595` | 2026-04-28 | ToM data pipeline + reward modes | LL/rule-based rewards, ToM parquet data | Core training pipeline |
| `6183fff` | 2026-04-24 | Config-driven data pipeline | Unified `build_dataset.py`, consolidated prompt templates | Reproducible datasets |
| `1b1d9e4` | 2026-04-04 | DailyDialog + Empathetic converters | Dataset conversion scripts | New dialogue sources |
| `bd1e48d` | 2025-04-21 | 3B model support | Working Qwen2.5-3B GRPO setup | Scale up |
| `afc1c13` | 2025-04-20 | NegTOM initial version | First ToM GRPO experiment | Project bootstrap |

## Bug Fixes

| Commit | Date | Fix | What changed | Why |
|--------|------|-----|--------------|-----|
| `0ee87fc` | 2026-07-06 | Reward stitching: no double `<think>` (Issue 2) | `response_parser.py` `split_thinking`: add the opening `<think>` only when the response does not already begin with one (and use `split(</think>, 1)` to keep everything after the first `</think>`) | Behavior/dialogue rollouts (rl_dataset, `add_generation_prompt=True`, no `generation_prefix`) emit their own `<think>`, so the old unconditional prepend fed the reward model a malformed `<think><think>…</think>` context and inflated `thinking_length`. Direct-ToM rollouts (generation_prefix `<think>`) still get the tag added. Covered by `tests/reward_score/test_response_parser.py` |
| `0ee87fc` | 2026-07-06 | Actor-as-RM response_mask boundary (Issue 3) | `fsdp_workers.py` `_build_actor_rm_inputs`: `response_mask[0, thinking_length-1 : full_len] = 1` (was `thinking_length:`), matching the frozen path `_switch_chat_template` | The two RM modes scored a response window shifted by one token, so actor-as-RM and frozen-RM rewards were not comparable. The `-1` compensates for the assistant-turn terminator that thinking-only re-tokenization appends. Verified with real tokenizers (Qwen/Gemma) that the corrected boundary scores exactly one extra leading token vs the old one and matches the frozen convention. NB: Gemma still under-covers the answer by >1 token (Issue 6, deferred) |
| _pending_ | 2026-07-05 | Repair `SIMPLE_USER_TEMPLATE` import after prompt-template trim | Renamed `SIMPLE_USER_TEMPLATE` → `USER_TEMPLATE_SIMPLE` references in `dialogue_converter_base.py`, `convert_dailydialog.py`, `convert_empathetic_dialogues.py`, `convert_conversations_gone_awry.py`, and updated removed `prompt_style` defaults (`cot`→`simple`) in `build_dataset.py` | The prompt-template trim (`PROMPT_STYLES` reduced to `simple`/`prefix_hint`, `SIMPLE_USER_TEMPLATE` renamed) left the converters importing a name that no longer exists → `ImportError` on any data-gen run |
| _pending_ | 2026-07-05 | Delete dead duplicate `merge_negTOM.py` | Removed `examples/data_preprocess/merge_negTOM.py` — a byte-for-byte duplicate of the pre-modernization `merge_tom.py` | Unreferenced anywhere in the repo and wrote the same `ToM_test_HiExTi*.parquet` filenames using the legacy baked-ChatML format — a landmine that would clobber the eval file with the broken (double-wrapping, Qwen-only) format if run |
| _pending_ | 2026-07-05 | Modernize `merge_tom.py` to model-agnostic messages (reproduce v3 eval) | `examples/data_preprocess/merge_tom.py`: replaced `make_prefix` (which baked a fully-rendered Qwen ChatML string into a single `user` message) with `make_messages` emitting `[{system},{user}]` chat messages; system prompt built from `cot_eval` (+ `--add_hint` room-witness note + new `--concise_answer` instruction); output filename derives from the active flags; added `answer`/`question`/`story` columns for parity. `python merge_tom.py --add_hint --concise_answer` now reproduces `ToM_test_HiExTi_hint_v3.parquet` byte-identically (verified 50/50 prompt match vs the committed file) | The `ToM_test_HiExTi_hint_v3.parquet` eval used by every training script had **no committed producer** — it was an ad-hoc chain (v1→v2 parse-ChatML→v3 add concise instruction). The committed `merge_tom.py` still emitted the legacy model-specific baked-ChatML format that requires `data.prompt_is_text=True` and double-wraps under `apply_chat_template` (breaking non-Qwen models); re-running it would clobber the eval file with the broken format. Note: `merge_negTOM.py` was an unreferenced exact-duplicate with the same legacy pattern (deleted in the row above) | | `experiments/tom_grpo_power_reward.sh`: after `source ~/.bashrc` added `eval "$($HOME/miniconda3/bin/conda shell.bash hook)"` + `conda activate tom` (matching `qwen2.5_3b_all_dialogue.sh`) | The script sourced `~/.bashrc` but never activated the `tom` conda env, so it ran system python → `ModuleNotFoundError: numpy` at launch. Required a throwaway patched copy during the sanity run; now the committed script launches directly | | `verl/utils/reward_score/tom_mc.py`: the `question_type=="exact"` branch now delegates to `explore_tom.check_answer_correctness` (exact-normalized OR prediction-ends-with-gold) instead of an ad-hoc "gold substring in prediction" test | Discovered during sanity RL runs: models answer open-ended ExploreToM-Infilled location questions with a full sentence ending in the gold span (e.g. "...in the conference room"). The ad-hoc containment could over-credit answers that merely mention the gold as context; delegating to the in-distribution explore_tom matcher makes the infilled robustness variant scored identically to the symbolic explore_tom eval (apples-to-apples) | `verl/workers/fsdp_workers.py`: when a rollout's thinking part alone fills/exceeds `max_length` (`thinking_length >= full_len`), `_switch_chat_template` used to `return response_mask` — a bare `Tensor` — mid-loop, aborting the whole batch. `compute_rm_score` then did `rm_data.batch` on that Tensor → `AttributeError: 'Tensor' object has no attribute 'batch'`. Now the sample is appended to the batch with a zero response mask and `invalid_response=True` (yielding the `invalid_penalty` downstream) and the loop `continue`s, so a proper `DataProto` is always returned | The frozen-RM stitching path (`USE_ACTOR_AS_RM=False`) hit this on Gemma2-2b where longer tokenization truncates the true next utterance for some samples; the 7B frozen-RM run never tripped it. One truncated sample crashed the entire GRPO step at step 1 |
| _pending_ | 2026-07-04 | Gemma2 config-driven attention + dynamic-bsz-aware actor-RM | `verl/workers/fsdp_workers.py`: `_build_model_optimizer` and the RewardModelWorker builder now read `attn_implementation` from config (default `flash_attention_2`; Gemma uses `eager`) instead of hard-coding flash-attn; `compute_actor_rm_score` was made dynamic-bsz aware (uses `rearrange_micro_batches` + `get_reverse_idx` when `ref.log_prob_use_dynamic_bsz`, mirroring `compute_rm_score`) | Gemma2 needs `eager` attention for logit soft-capping to match the vLLM rollout, but the builders hard-coded flash-attn. `compute_actor_rm_score` used a static equal-size split that OOMs with the eager/non-rmpad 256k-vocab forward and ignored the dynamic-bsz path used everywhere else, so its per-sequence eos-position score mapping had to reorder-then-reverse to stay correct |
| _pending_ | 2026-07-02 | Long-prompt truncation for merged data | Added `+data.truncation=left` to `experiments/qwen2.5_3b_all_dialogue.sh` | Corrected ThoughtTrace (`target_speaker: "user"`) has prompts up to ~6,249 tokens (>`max_prompt_length=2048`); default `truncation='error'` crashed the first training step (`NotImplementedError: sequence_length=6025 > max_length=2048`). Left-truncation is self-consistent (actor-as-RM recomputes reward on the truncated prompt; `subtract_baseline=False` so precomputed `answer_pp` is unused) and preserves recent context + generation prompt; affects only ~3.5% of samples | `fsdp_workers.py` coerces `None` `answer_pp` values to `0.0` before `torch.tensor` | Merged multi-dataset batches could contain rows with `answer_pp=None`, crashing reward computation (`TypeError: must be real number, not NoneType`) |
| _pending_ | 2026-06-29 | FANToM eval prompt double-wrapping | `prepare_fantom.py` now stores model-agnostic `[system,user]` messages instead of baked-in `<|im_start|>` ChatML; `fantom_test.parquet` regenerated | Baked Qwen ChatML got re-wrapped by `apply_chat_template` (nested system, dangling `<think>`) and broke non-Qwen models; clean messages template per-model |
| `3e42df7` | 2026-05-25 | FANToM eval script config | Match reference 17f config | Eval was diverging from training recipe |
| `f9a9d18` | 2026-05-31 | Double `<think>` tag parsing | Fixed parsing + eval answer extraction + actor-as-RM stitching | Qwen3 broke answer extraction |
| _pending_ | 2026-07-03 | Qwen3 HF forward missing QK-norm (NaN divergence) | Added per-head QK-RMSNorm (`self.q_norm`/`self.k_norm`, over `head_dim`, before RoPE) to `qwen3_flash_attn_forward` in `verl/models/transformers/qwen3.py` | The monkey-patched Qwen3 attention forward was a copy of the Qwen2 path and omitted Qwen3's QK-normalization. Qwen3 weights are trained expecting QK-norm; without it, unnormalized Q·Kᵀ magnitudes overflow in bf16 → NaN logits → `reward/mean:nan` → NaN advantages/grad → actor weights corrupt (observed: `q_norm.weight mean=nan`) → degenerate max-length generations → vLLM CPU-swap abort. Qwen3-1.7B all_dialogue run crashed at step 3; rollout side (new `qwen3.py` vLLM model) already had QK-norm, so only the training/RM forward was missing it |
| _pending_ | 2026-07-03 | Actor-as-RM 0/0 NaN on empty-response rollouts | In `verl/workers/fsdp_workers.py` guarded `log_prob / response_mask.sum(-1)` with `.clamp(min=1)` in both `_actor_rm_forward` and `_forward_micro_batch`, and added `torch.where(response_token_count>0, ...)` + `torch.nan_to_num(...)` before the final reward clamp | A degenerate rollout whose thinking fills the RM context leaves zero response tokens after right-truncation to `src_max_length` → `response_mask.sum()==0` → `0/0 = NaN`. When such a response still contains `</think>` it is *not* flagged `invalid_response`, so `masked_fill_` never overwrites the NaN; the final `torch.clamp` preserves NaN → a single sample poisons `reward/mean` → NaN advantages/grad → crash (`Aborted due to the lack of CPU swap space`). Qwen3-1.7B all_dialogue reached step 9 (finite rewards) then crashed again; this makes the reward robust regardless of generation degeneracy |
| `f87c1cf` | 2025-04-17 | `self.prompt_is_text` usage | Use flag correctly | Prompt handling bug |
| `9ed1268` | 2025-04-08 | Misc fix commit | Early fixups | Stabilize initial pipeline |

## Documentation / Experiment Logs

| Commit | Date | Update |
|--------|------|--------|
| _pending_ | 2026-07-06 | Added an "Eval set statistics" section to `docs/EVAL_BENCHMARKS.md` with two per-subtype tables: Table 1 = full suite (`VAL_SUITE=full`, 26 subtypes / 10 files / 39,988 rows, using fantom_test_50pct + full-fantom note) with per-subtype rows + role; Table 2 = `subsample300` (25 subtypes × 300 = 7,500, ullman excluded) with sampled/available/coverage% per subtype and the ~3.5pp CI note |
| `5805ef4` | 2026-07-05 | Added a "Default Data Generation Parameters" section to `docs/skill_adding_dataset.md`: documents sensible defaults (`min_turns: 6`, `min_response_words: 3`, `max_response_words: 100`, `turn_order: random`, `target_speaker: user` for human↔AI data, `split: train`) that experiments/grid-search may override. Reconciled the Best Practices list and Step-3 example config to match (was `min_response_words: 10`, no max/target_speaker) |
| _pending_ | 2026-07-05 | Trimmed `docs/ENVIRONMENT_SETUP.md` to the current working `tom` env only (supports Qwen2.5 / Qwen3 / Gemma-2): removed the multi-env history table (`tom2`/`tom3`/`yolo`/`.venv`/`~/venv`), the tom3 uncommitted-patch history note, the Round-20/`logs/` narrative, and the Qwen2.5-only transformers caveat; kept canonical install, transformers 4.51.3 note, Qwen3 vllm patch, versions, run/hardware notes |
| _pending_ | 2026-07-05 | Paper-branch cleanup (Phase 2c): fixed stale references — deleted `docs/training_scripts_reference.md` (documented 6 deleted round-specific scripts); repointed `CLAUDE.md` summary-md path `project_planning/results/` → `experiments_logs/`, corrected converter count, dropped the deleted-doc link, and marked `grpo_tuning_changelog.md` frozen/archival; updated `docs/ENVIRONMENT_SETUP.md` + `docs/skill_adding_dataset.md` example scripts to surviving launchers |
| _pending_ | 2026-07-05 | Paper-branch cleanup (Phase 2b): added a `join-experiments` onboarding skill for a new agent joining the multi-agent execution pool (only claim `Not-started` rows, update tracker `Status` *before* acting incl. `Awaiting-input`-then-ask, re-read/append to avoid clobbering concurrent edits, don't mutate shared `verl`/`dcfg_*`). Introduced `experiments_logs/<RUN_NAME>.md` as the per-run self-contained log (hypothesis/impl/debugging/findings/metadata); repointed `launch-experiment`/`log-results` from `project_planning/results/` to `experiments_logs/` |
| _pending_ | 2026-07-05 | Paper-branch cleanup (Phase 2): rewrote the 3 Claude skills (`launch-experiment`, `check-training`, `log-results`) from the retired 17f/round-changelog format to the tracker-driven protocol, and added a new `claim-experiment` skill (atomic claim + dependency gate). Skills now execute `project_planning/BeRL_experiments_tracker.md` (claim row → launch generic launcher → check health → write `Results summary` + `project_planning/results/<RUN_NAME>.md`) |
| _pending_ | 2026-07-05 | Paper-branch cleanup (Phase 1): deleted 11 obsolete round-specific experiment scripts, 11 obsolete `pipeline_config_*` variants, and 17 stale one-off docs (DAILYDIALOG_*/EMPATHETIC_*/summaries); archived Round 1–20 narrative to `project_planning/HISTORY_rounds_1-20.md`; slimmed `CLAUDE.md` to point at `project_planning/` as source of truth. Pruned ~1.53T of pre-paper checkpoints/logs/wandb (git-ignored; paper runs start fresh) |
| `057ebaf` | 2026-05-28 | Round 19 results in changelog |
| `c20b729` | 2026-05-27 | 7B direct ToM baseline in CLAUDE.md |
| `9543672` | 2026-05-27 | 7B scaling results |
| `9b9d3d4` | 2026-05-26 | Round 19 FANToM/CGA results |
| `e16051e` | 2026-05-18 | Round 18 7B scaling + Claude skills |
| `ceb8db3` | 2026-05-08 | Initial CLAUDE.md cross-session context |
| `2923c49` | 2026-05-07 | Round 17i-j prompt ablations |
